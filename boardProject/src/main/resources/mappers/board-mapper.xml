<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.kh.project.board.model.mapper.BoardMapper">
  
  <!-- 게시판 종류 조회 메서드 -->
  <!-- 
  	MAP으로 가져올 경우, 
  	{BOARD_CODE: 1, BOARD_NAME: 자유게시판} 
  	이와 같은 형식으로 가져와지기 때문에 별칭을 카멜케이스를 적용하여 불러와야 한다.
  	-->
  <select id="selectBoardTypeList">
  	SELECT BOARD_CODE "boardCode",
  		   BOARD_NAME "boardName"
  	FROM "BOARD_TYPE"
  	ORDER BY BOARD_CODE
  </select>
  
  <select id="getListCount">
  	SELECT COUNT(*)
  	FROM "BOARD"
  	WHERE BOARD_DEL_FL = 'N'
  	AND BOARD_CODE = #{boardCode}
  </select>
  
  <!-- 
  	<![CDATA["문자열"]]>
  	해당 태그 내부에 작성된 문자열은 태그와 같은 특수 기호로 해석하지 말고
  	문자 그대로 인식하라는 태그/명령 (순수 문자 데이터임을 지정)
   -->
  
  <!-- 특정 게시판의 지정된 페이지 목록 조회 -->
  <!-- DB SQL 에 쿼리 설명 주석 있음 -->
  <select id="selectBoardList">
  	SELECT BOARD_NO, BOARD_TITLE, MEMBER_NICKNAME, READ_COUNT,
		
		(SELECT COUNT (*)
		 FROM "COMMENT" C
		 WHERE C.BOARD_NO = B.BOARD_NO) COMMENT_COUNT, 
		 
		 (SELECT COUNT (*) 
		  FROM "BOARD_LIKE" L
		  WHERE L.BOARD_NO = B.BOARD_NO) LIKE_COUNT, 
		<![CDATA[
		 CASE 
		 
	   	 WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 / 60 
	   	 THEN FLOOR ((SYSDATE - BOARD_WRITE_DATE) * 24 * 60 * 60) || '초 전'
	   	 
		 WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 
		 THEN FLOOR ((SYSDATE - BOARD_WRITE_DATE) * 24 * 60) || '분 전'
		 
		 WHEN SYSDATE - BOARD_WRITE_DATE < 1 
	  	 THEN FLOOR ((SYSDATE - BOARD_WRITE_DATE) * 24) || '시간 전'
	  	 
	  	 ELSE TO_CHAR(B.BOARD_WRITE_DATE, 'YYYY-MM-DD') 
	  	 
		 END BOARD_WRITE_DATE
		 ]]>	 	
	FROM "BOARD" B
	JOIN "MEMBER" USING(MEMBER_NO)
	WHERE BOARD_DEL_FL = 'N'
	AND   BOARD_CODE = #{boardCode}
	ORDER BY BOARD_NO DESC
  </select>
  
  <!-- 
  	동적 SQL (mybatis 제공) 
  	
  	1. <choose> : 조건문을 작성할 영역 지정, 내부에 <when>, <otherwise> 태그 지정 가능
  	2. <when> : if, else if 역할의 태그, "test" 속성으로 조건식 작성
  	3. <otherwise> : else 역할의 태그  
  -->
  
  <!-- 검색 조건 맞는 게시글 수 조회 -->
  <select id="getSearchCount">
  	
  	SELECT COUNT(*)
  	FROM "BOARD"
  	
  	<!-- 작성자 검색인 경우 MEMBER 테이블 JOIN 필요. 만약 key==w 가 넘어오지 않았다면 아래 if 태그 실행되지 않을 것 -->
  	<!-- test 속성에 value 입력시 반드시 쌍따옴표로 감싸주어야 하길래 바깥 쪽 속성 작성 부분을 홑따옴표로 감싸준다 -->
  	
  	<if test='key == "w"'>
  		JOIN "MEMBER" USING(MEMBER_NO)	
  	</if>
  	 
  	WHERE BOARD_DEL_FL = 'N'
  	AND BOARD_CODE = #{boardCode}
  	<choose>
  		<!-- 제목 검색 (key값 "t")  -->
  		<when test='key == "t"'>
  			AND BOARD_TITLE LIKE '%' || #{query} ||'%'
  		</when>
  		
  		<!-- 내용 검색 (key값 "c")  -->
  		<when test='key == "c"'>
  			AND BOARD_CONTENT LIKE '%' || #{query} ||'%'
  		</when>  		
  		
  		<!-- 제목+내용 검색 (key값 "tc")  -->
  		<when test='key == "tc"'>
  			AND(
	  			BOARD_TITLE LIKE '%' || #{query} ||'%'
	  			OR
	  			BOARD_CONTENT LIKE '%' || #{query} ||'%'
  			)
  		</when> 
  		  		
  		<!-- 작성자 검색 (key값 "w")  -->
		<otherwise>
			AND MEMBER_NICKNAME LIKE '%' || #{query} || '%'
		</otherwise>
  	</choose>
  </select>
  
  <!-- 검색 결과 목록 조회 -->
  <select id="selectSearchList">
  	SELECT BOARD_NO, BOARD_TITLE, MEMBER_NICKNAME, READ_COUNT,
		
		(SELECT COUNT (*)
		 FROM "COMMENT" C
		 WHERE C.BOARD_NO = B.BOARD_NO) COMMENT_COUNT, 
		 
		 (SELECT COUNT (*) 
		  FROM "BOARD_LIKE" L
		  WHERE L.BOARD_NO = B.BOARD_NO) LIKE_COUNT, 
		<![CDATA[
		 CASE 
		 
	   	 WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 / 60 
	   	 THEN FLOOR ((SYSDATE - BOARD_WRITE_DATE) * 24 * 60 * 60) || '초 전'
	   	 
		 WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 
		 THEN FLOOR ((SYSDATE - BOARD_WRITE_DATE) * 24 * 60) || '분 전'
		 
		 WHEN SYSDATE - BOARD_WRITE_DATE < 1 
	  	 THEN FLOOR ((SYSDATE - BOARD_WRITE_DATE) * 24) || '시간 전'
	  	 
	  	 ELSE TO_CHAR(B.BOARD_WRITE_DATE, 'YYYY-MM-DD') 
	  	 
		 END BOARD_WRITE_DATE
		 ]]>	 	
	FROM "BOARD" B
	JOIN "MEMBER" USING(MEMBER_NO)
	WHERE BOARD_DEL_FL = 'N'
	AND   BOARD_CODE = #{boardCode}
	
	<choose>	
  		<!-- 제목 검색 (key값 "t")  -->
  		<when test='key == "t"'>
  			AND BOARD_TITLE LIKE '%' || #{query} ||'%'
  		</when>
  		
  		<!-- 내용 검색 (key값 "c")  -->
  		<when test='key == "c"'>
  			AND BOARD_CONTENT LIKE '%' || #{query} ||'%'
  		</when>  		
  		
  		<!-- 제목+내용 검색 (key값 "tc")  -->
  		<when test='key == "tc"'>
  			AND(
	  			BOARD_TITLE LIKE '%' || #{query} ||'%'
	  			OR
	  			BOARD_CONTENT LIKE '%' || #{query} ||'%'
  			)
  		</when> 
  		  		
  		<!-- 작성자 검색 (key값 "w")  -->
		<otherwise>
			AND MEMBER_NICKNAME LIKE '%' || #{query} || '%'
		</otherwise>
  	</choose>
	
	ORDER BY BOARD_NO DESC
  </select>
  
  <!-- @@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@ -->
  
  <!--
    2.  
  	resultMap 태그 :
  	1) 조회된 컬럼명과 DTO 필드명이 일치하지 않을 때 매핑 시켜주는 역할
  	 -> 하지만 자동으로 매핑되게끔 mybatis-config.xml 에 이미 설정해둔 상태
  	2) 따라서 여기서의 역할은 <collection> 태그를 내부에 작성하여
  	   여러 행 결과가 조회되는 SELECT 수행 후 그 결과를 지정하는 DTO 필드에 대입할 수 있게 해줌
  	3) resultMap의 Collection 태그를 이용한 SQL 호출 시 반환 타입 반드시 resultType 속성 이용하여 명시해야 함
   -->
  
  <resultMap type="Board" id="board_rm">
  	<id property="boardNo" column="BOARD_NO" />
  		
  	<!-- resultMap 의 type 속성 : id와 연결되는 SQL이 담길 객체(Board) -->
  	<!-- column 속성 : resultMap 태그의 id와 연결되는(board_rm) SQL의 대상이 되는 테이블(BOARD)의 PK 컬럼(BOARD_NO) -->
  	<!-- property 속성 : column(BOARD_NO)이 DTO에서 불리는 이름(boardNo) -->
  	
  	<!-- 
  	collection 태그 : SELECT로 조회된 결과를 컬렉션에 담아 지정된 필드에 세팅 
 	 property : 결과를 담을 변수 = List를 담을 DTO 필드명
 	 column : 호출할 SELECT 구문에 전달해줄 PARAMETER 가 될 컬럼명(DB표기법). 즉, 조회 결과(부모쿼리 수행결과) 중 지정된 컬럼 값을 파라미터로 전달함
 	 select : 호출할 SELECT 구문의 id
 	 javaType : 결과값을 저장할 형식(여기서는 List이고, 패키지명까지 전부 작성
 	 ofType : 위 javaType 에 넣을 것. 즉, Generic(타입제한), 여기선 BoardImg 	 
  	-->
  	
  	<!-- 3. 해당 게시글 이미지 목록 조회 후 필드에 저장 -->
  	<collection property="imageList" 
  				column="BOARD_NO" 
  				select="selectImageList" 
  				javaType="java.util.ArrayList" 
  				ofType="BoardImg" />

	<!-- 4. 해당 게시글 댓글 목록 조회 후 필드에 저장 -->
  	<collection property="commentList"
  				column="BOARD_NO"
  				select="selectCommentList"
  				javaType="java.util.ArrayList"
  				ofType="Comment" />
  </resultMap>
  	
  	<!-- 5. 상세 조회한 게시글의 이미지 목록 조회 -->
  	 <!-- resultType 필수 작성 -->
	<select id="selectImageList" resultType="BoardImg">
		SELECT * FROM "BOARD_IMG"
		WHERE BOARD_NO = #{boardNo}
		ORDER BY IMG_ORDER
	</select>
  	
  	<!-- 6. 상세 조회한 게시글의 댓글 목록 조회(계층형 쿼리 사용) -->
  	 <!-- resultType 필수 작성 -->
  	<select id="selectCommentList" resultType="Comment">
  	SELECT LEVEL, C.* 
	FROM 
		(SELECT COMMENT_NO, COMMENT_CONTENT, 
		 TO_CHAR(COMMENT_WRITE_DATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"') COMMENT_WRITE_DATE,
		 BOARD_NO, MEMBER_NO, MEMBER_NICKNAME, PROFILE_IMG, PARENT_COMMENT_NO, COMMENT_DEL_FL
		 FROM "COMMENT" 
		 JOIN MEMBER USING(MEMBER_NO)
		 WHERE BOARD_NO = #{boardNo}
		) C 
	WHERE COMMENT_DEL_FL = 'N' 
	OR 0 != ( 
			 SELECT COUNT(*) FROM "COMMENT" SUB
			 WHERE SUB.PARENT_COMMENT_NO = C.COMMENT_NO 
			 AND COMMENT_DEL_FL = 'N'
			)
	START WITH PARENT_COMMENT_NO IS NULL 
	CONNECT BY PRIOR COMMENT_NO = PARENT_COMMENT_NO  
	ORDER SIBLINGS BY COMMENT_NO
  	</select>
  	
  <select id="selectOne" resultMap="board_rm"> <!-- 1. mapper로부터 sql 수행 위임받고, 수행 후 결과를 resultMap의 "board_rm"으로 전송 -->
	SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, BOARD_CODE, READ_COUNT, MEMBER_NO, MEMBER_NICKNAME, PROFILE_IMG,
		   TO_CHAR(BOARD_WRITE_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') BOARD_WRITE_DATE,
		   TO_CHAR(BOARD_UPDATE_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') BOARD_UPDATE_DATE,
		
		   (SELECT COUNT(*)
		   FROM "BOARD_LIKE"
		   WHERE BOARD_NO = #{boardNo}) LIKE_COUNT, <!-- 좋아요 개수 -->
		
		   (SELECT IMG_PATH || IMG_RENAME <!-- 문자열 이어써서 경로 만들어주기 -->
		   FROM "BOARD_IMG"
		   WHERE BOARD_NO = #{boardNo}
		   AND IMG_ORDER = 0 ) THUMBNAIL, <!-- 썸네일 이미지(이미지 순서 0번)  -->
		
		   (SELECT COUNT(*)
		   FROM "BOARD_LIKE"
		   WHERE BOARD_NO = #{boardNo}
		   AND MEMBER_NO = #{memberNo}) LIKE_CHECK <!-- 좋아요 여부 확인, 로그인  -->
		
	FROM "BOARD"
	JOIN "MEMBER" USING(MEMBER_NO)
	WHERE BOARD_DEL_FL = 'N'
	AND BOARD_CODE = #{boardCode}
	AND BOARD_NO = #{boardNo}
  </select>
  
<!-- @@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@ -->
  
  <update id="updateReadCount">
  	UPDATE "BOARD"
  	SET READ_COUNT = READ_COUNT + 1
  	WHERE BOARD_NO = #{boardNo}
  </update>
  
  <select id="selectReadCount">
  	SELECT READ_COUNT
  	FROM "BOARD"
  	WHERE BOARD_NO = #{boardNo}
  </select>
  
  
  <!-- 좋아요 관련 -->
  
  <delete id="deleteBoardLike">
	DELETE FROM "BOARD_LIKE"
	WHERE MEMBER_NO = #{memberNo}
	AND	  BOARD_NO = #{boardNo}
  </delete>
 
  <insert id="insertBoardLike">
  	INSERT INTO "BOARD_LIKE"
  	VALUES(#{memberNo}, #{boardNo})
  </insert> 
  
  <select id="selectLikeCount">
  	SELECT COUNT(*) FROM "BOARD_LIKE"
  	WHERE BOARD_NO = #{boardNo}
  </select>
  
  
</mapper>
